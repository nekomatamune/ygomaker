name: Gradle CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: setup-java
      uses: olafurpg/setup-scala@v3
      with:
        java-version: 1.8
    - name: Build with Gradle
      run: ./gradlew build
